spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/contact
    username: postgres
    password: password

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  main:
    allow-bean-definition-overriding: true

server:
  port: 9090